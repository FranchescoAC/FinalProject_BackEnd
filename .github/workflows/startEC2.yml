name: SSH Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/ssh@v1
        with:
          host: ${{ secrets.ec2_host }}
          username: ubuntu
          key: ${{ secrets.ec2_key }}
          script: |
            echo "SSH works!"