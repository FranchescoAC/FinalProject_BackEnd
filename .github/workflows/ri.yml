name: Main Workflow
on:
  push:
    branches: [dev]

jobs:
  DeleteBus:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Delete-Bus
        uses: .github/workflows/bus-management/Delete-Bus.yml # Ruta relativa

jobs2:
  RegisterBus:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Register Bus
        uses: .github/workflows/bus-management/Register-Bus.yml  # Ruta relativa

jobs3:
  UpdateBus:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Update Bus Availability
        uses: .github/workflows/bus-management/Update-Bus.yml  # Ruta relativa