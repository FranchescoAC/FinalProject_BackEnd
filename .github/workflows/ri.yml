name: Main Workflow
on:
  push:
    branches: [dev]

jobs:
  Register:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Register-Bus
        uses: ./.github/workflows/bus-management/Register-Bus.yml # Ruta RELATIVA CORRECTA

jobs:
  Delete:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Delete-Bus
        uses: ./.github/workflows/bus-management/Delete-Bus.yml # Ruta RELATIVA CORRECTA

jobs:
  Update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Update-Bus
        uses: ./.github/workflows/bus-management/Update-Bus.yml # Ruta RELATIVA CORRECTA