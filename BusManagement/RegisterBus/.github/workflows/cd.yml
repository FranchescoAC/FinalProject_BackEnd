name: CI/CD for RegisterBus

on:
  workflow_dispatch:
    inputs:
      service:
        required: true
        description: "Name of the service"
      branch:
        required: false
        description: "Branch to use"
  push:
    branches: ["dev"]

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build and push Docker image for RegisterBus
        run: |
          echo "Building Docker image for ${{ github.event.inputs.service }}"
          # Aquí va tu script personalizado
